<email> bruce barnett subject re title xterm inreplyto <email> message thu <number> apr <number> <number><number><number> gmt nntppostinghost grymoirecrdgecom replyto <email> organization ge corp r d schenectady ny <<email> <<email> article <<email> <email> mark lanzo writes hint sun os users use usr0binecho instead binecho cshs builtin echo otherwise youll embed literal esc bel characters string instead using convenient octal sequences bad idea using usr0binecho slower builtin echo execute hostname per shell window read one extra file manipluating titles require executing extra programs oh yes execute programs per system stores file next time <cur>hostname setenv hostname hostname endif figure generate escape bell echo commands without line terminator may done variable e set executed script system <cur>e echo already set echo variables>devtty else f ~echo<cur>hostname source ~echo<cur>hostname else echo n |wc l == <number> echo built echo bsd >devtty berkeley style echo echo set ech = echo n >~echo<cur>hostname echo set e = echo | tr >> ~echo<cur>hostname echo set b = echo | tr >> ~echo<cur>hostname echo set n = >> ~echo<cur>hostname source ~echo<cur>hostname else echo built echo sysv >devtty echo set ech = echo >~echo<cur>hostname echo set e = >> ~echo<cur>hostname echo set b = >> ~echo<cur>hostname echo set n = c >> ~echo<cur>hostname source ~echo<cur>hostname endif using shelltool cmdtool xterm duplicate aliases avoid problems <cur>term =~ sun sun aliases alias header <cur>ech <cur>el<number><cur>e<cur>n alias iheader <cur>ech <cur>el<number><cur>e<cur>n else <cur>term =~ xterm alias header <cur>ech <cur>e<number><number><cur>b<cur>n alias iheader <cur>ech <cur>e<number><number><cur>b<cur>n endif bruce barnett <<email> uunetcrdgw0barnett